"""Atlas — ML context layer for Insight (Layer M).

Atlas runs only models that produce **descriptive context**. It never
produces betting predictions, win-probability outputs, or tipster
behaviour. See the README for the complete list of forbidden outputs.
"""
